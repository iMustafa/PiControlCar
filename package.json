{
    "name": "comms-root",
    "private": true,
    "version": "1.0.0",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "build": "echo Skipping root build; using heroku-postbuild",
        "build:client": "npm run build --prefix client",
        "build:server": "npm run build --prefix server",
        "start": "node server/dist/index.js",
        "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm ci --prefix client && npm run build --prefix client && NPM_CONFIG_PRODUCTION=false npm ci --prefix server && npm run build --prefix server"
    }
}